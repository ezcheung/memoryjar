import React from 'react';
import {
  StyleSheet
} from 'react-native';

const Styles = StyleSheet.create({
  memory : {
    borderBottomColor : 'rgba(0, 0, 0, 0.65)',
    borderBottomWidth : 1,
    padding : 10
  },
});

export {Styles};